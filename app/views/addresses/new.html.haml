= render_flash
= simple_form_for @address, html: { class: 'offset2 span21 form-inline well' } do |f|
  %legend
    = "New Address for #{@parent.name1}"
  = render 'fields', f: f
  .form-actions
    = f.button :submit, 'Save', class: 'btn btn-primary'
    = link_to 'Cancel', edit_polymorphic_path(@parent), class: 'btn btn-warning'