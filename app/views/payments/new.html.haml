= render_flash
= simple_nested_form_for @payment, html: { class: 'form-inline well', id: 'payment' } do |f|
  %legend
    New Payment
  = render 'fields', f: f
  .form-actions
    = f.button :submit, 'Save', class: 'btn btn-primary'
    = link_to 'Cancel', Payment.not_matched.last ? edit_payment_path(Payment.not_matched.last) : root_path, class: 'btn btn-warning'

