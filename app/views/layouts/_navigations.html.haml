.navbar-inner
  .container
    %ul.nav
      = menu 'Accounting' do
        = menu_child 'Chart of Account', edit_account_type_path(AccountType.first)
      = menu 'Sales'
      = menu 'Purchases'
      = menu 'Bank'
      = menu "Payroll"
