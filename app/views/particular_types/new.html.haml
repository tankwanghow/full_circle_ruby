= simple_form_for @particular_type, html: { class: 'form-horizontal well span7 offset2' } do |f|
  %legend
    New Particular Type
  = render 'fields', f: f
  .form-actions
    = f.button :submit, 'Save', class: 'btn btn-primary'
    = link_to 'Cancel', new_particular_type_path, class: 'btn btn-warning'